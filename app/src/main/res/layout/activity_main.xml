<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="16dp"
    android:paddingRight="16dp"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="lifeproduction.com.lifeproduction.MainActivity">

    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:layout_above="@+id/btnLoginRegister"
        android:layout_centerVertical="true"
        android:layout_marginBottom="20dp"
        android:background="@color/colorPrimary" />

    <Button
        android:id="@+id/btnLoginRegister"
        style="@style/Widget.AppCompat.Button.Colored"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignStart="@+id/btnContactUs"
        android:layout_centerVertical="true"
        android:layout_marginBottom="25dp"
        android:background="@color/colorPrimary"
        android:elevation="10dp"
        android:text="Login / register"
        android:visibility="visible" />


    <Button
        android:id="@+id/btnContactUs"
        style="@style/Widget.AppCompat.Button.Colored"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignStart="@+id/btnView"
        android:layout_centerVertical="true"
        android:layout_marginTop="35dp"
        android:background="@color/colorPrimary"
        android:elevation="10dp"
        android:text="Contact us" />

    <Button
        android:id="@+id/btnBooking"
        style="@style/Widget.AppCompat.Button.Colored"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"
        android:layout_centerVertical="true"
        android:layout_marginTop="45dp"
        android:background="@color/colorPrimary"
        android:elevation="10dp"
        android:text="Booking" />

    <Button
        android:id="@+id/btnView"
        style="@style/Widget.AppCompat.Button.Colored"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/btnBooking"
        android:layout_centerVertical="true"
        android:layout_marginTop="35dp"
        android:background="@color/colorPrimary"
        android:elevation="10dp"
        android:text="View photos / videos" />
</RelativeLayout>
